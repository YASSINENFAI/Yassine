# 🚀 كيفية رفع الموقع والشات بوت على Vercel

بما أن مشروعك عبارة عن موقع HTML/JS مع وظيفة Serverless (ملف `api/chat.js`)، فإن Vercel هو المكان المثالي له. الكود الذي قمنا بكتابته مصمم ليعمل مباشرة.

إليك الخطوات البسيطة جداً لجعله يعمل:

## 1. التجهيز (Environment Variables)
هذه أهم خطوة لحماية مفتاح API الخاص بك.

1. اذهب لى لوحة تحكم Vercel (Dashboard).
2. اختر مشروعك (Project).
3. انتقل إلى **Settings** (الإعدادات) > **Environment Variables**.
4. أضف متغيراً جديداً:
   - **Key**: `GROQ_API_KEY`
   - **Value**: `...الصق مفتاح Groq الخاص بك هنا...`
5. اضغط **Save**.

## 2. هيكلية الملفات (تأكد منها)
لقد قمت بإنشاء الملفات الصحيحة لك، وتأكد أن هيكلية المجلد كالتالي:
```text
/
├── index.html        (الصفحة الرئيسية وبها كود استدعاء الشات)
├── css/
│   ├── styles.css
│   └── chat-widget.css   (تصميم الشات)
├── js/
│   ├── main.js
│   └── chat-widget.js    (كود الشات الأمامي)
└── api/
    └── chat.js       (الكود الخلفي الآمن - Serverless Function)
```
*ملاحظة: بما أننا لا نستخدم Next.js App Router، فإن ملف `api/chat.js` هو الطريقة الصحيحة (Legacy/Standard Serverless) وهو ما قمنا باستخدامه.*

## 3. الرفع (Deploy)
إذا كنت تستخدم GitHub:
1. قم بعمل `Commit` و `Push` للتغييرات الجديدة.
2. سيقوم Vercel باكتشاف التغييرات وبناء الموقع ونشر دالة الـ API تلقائياً.

## 4. التجربة
بعد انتهاء الـ Deploy (يستغرق ثواني):
1. افتح رابط الموقع الجديد.
2. اضغط على أيقونة المحادثة 💬.
3. جرب إرسال "مرحباً".
4. إذا رد البوت، فمبروك! لقد نجحت.

---

### ❓ حل المشاكل (Troubleshooting)
- **إذا ظهر خطأ في الشات (Error):**
  - اذهب إلى Vercel Dashboard > Project > **Logs**.
  - فلتر الـ Logs لترى أخطاء الـ Functions.
  - تأكد أن اسم المتغير `GROQ_API_KEY` مكتوب بشكل صحيح تماماً.

- **إذا لم يظهر زر الشات:**
  - تأكد أنك قمت بعمل Refresh للصفحة (احذف الكاش `Ctrl + Shift + R`).
  - تأكد من وجود ملفات `chat-widget.css` و `chat-widget.js` في مجلداتهم.
